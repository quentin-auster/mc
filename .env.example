# Development-only credentials. Do not reuse them outside the local Compose stack.
MC_POSTGRES_DB=mc
MC_POSTGRES_USER=mc
MC_POSTGRES_PASSWORD=mc_dev_only
MC_POSTGRES_PORT=5432
MC_MINIO_ROOT_USER=mcdev
MC_MINIO_ROOT_PASSWORD=mc_dev_only_password
